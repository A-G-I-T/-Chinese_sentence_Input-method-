# -Chinese_sentence_Input-method-
本项目实现了一种轻量级输入法，结合C++ Windows底层键盘钩子和Python后端。按键由C++钩子捕获，通过单次TCP连接发送至Python服务器。服务器将用户输入历史存入简单数据库（如SQLite），并利用这些个人数据进行上下文预测或纠错。例如，可建议之前输入过的短语或学习用户常用模式。数据库完全由用户自身输入构建。非常适合实验个性化输入辅助或隐私优先的输入日志。
